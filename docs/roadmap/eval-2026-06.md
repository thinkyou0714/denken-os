# denken-os 全体評価とロードマップ — 2026-06（現 main ベース）

16 エージェントによる実コード精読評価（8 次元 × 並列）+ 敵対的検証（各 high/critical を別エージェントが
実コードで再検証）を、現 `origin/main`（zod v4、governance/監査 #15/#16/#18 反映後）に対して実施した記録。
本書は実装済み（本リリース）と今後（ロードマップ）の単一参照点。

## 評価サマリ

現 main は pre-alpha ながら作り込みが高い：決定論 solver（答案を LLM に出させずコードで算出）、
schema/exam↔subject 整合、RLS DDL、固定シードの不変条件スイープ、CI 品質ゲート、governance hardening。
本評価では、その設計の核心（反ハルシネーション・SR 診断・永続化 parity）に残る根本原因バグを検出・修正した。

## 実装済み（本リリース）

### 検証済み root-cause バグ（敵対的検証で isReal=true）

| ID | バグ | 修正 |
|---|---|---|
| F1/DI-1 | 反ハルシネーションゲート `narrationMatchesAnswer` が「解説中のどこかの数値」一致で合格（最終値非アンカー）。cos=1.0 変圧器では答え=p が中間式に必ず現れ、最終値を誤っても素通り（設計の核心保証が無効化） | 結論の最終値（`=` 以降が数値+単位だけのステップの最後）にアンカー。`validate.test.ts` に偽陽性負テスト追加 |
| SCHED-2 | 弱点診断が `dueMs=最終解答時刻` を使い、overdue=放置日数に化け、最近やった topic を不利・習得済の放置 topic を弱点に誤判定（SR セマンティクス逆転） | スケジューラの実 due を渡せるよう拡張（`app.ts` が `allReviews` から供給）。`diagnosis.test.ts` に未来due/過去due・習得済 vs 低正答率の順位テスト |

### 高確度 root-cause バグ / 不足（評価で検出、独立確認のうえ修正）

| ID | 内容 | 修正 |
|---|---|---|
| B2 | `param.value` が `realistic_range` 内に制約されない | zod superRefine + validate-problems の両輪で value∈[min,max] 強制 |
| B3 | FSRS `stability/difficulty` 列はあるのに ReviewState 型・Supabase mapping で欠落 | 型に通し往復で保持（file/in-memory は JSON で自動保持） |
| STORE-1 | Supabase が `Problem.params` を消失（行・列とも無し）→ file との parity 破れ | params 列（migration 0003）+ mapper + 往復テスト |
| SCHED-1 | `FsrsScheduler` が `Scheduler` 非実装（Card 止まり）で永続化経路に乗らず FSRS が dead code | `FsrsReviewScheduler`（ReviewState in/out）+ Card⇔ReviewState 変換。store 往復で FSRS 状態が保持され続きから同予定を再現 |
| DI-2 | 記述採点の部分文字列誤マッチ（'4.6' が '14.65' に一致） | 語境界一致に変更 |
| B5/CI-1 | 供給網の standing 監査が CI に無い | validate.yml に本番依存 `npm audit --audit-level=high --omit=dev` gate（dev は可視化） |
| SEC-1 | deploy-pages が validate と独立 → CI 赤でも Pages 公開されえた | deploy の build を `npm run verify` ゲートに連動 |
| CI-2 | coverage が `lib/**` のみ＝出荷 web ロジックが閾値保護外 | `web/src/**` を計測対象に追加（DOM 配線 app.ts は除外） |
| security | CSP 不在 / service_role の client 混入検査なし | index.html に CSP meta、build-web がサーバ専用シークレット混入でビルド失敗 |
| a11y | 動的領域に aria-live なし / 次問フォーカス移動なし / focus-visible なし | role=group・aria-live・focus 移動・focus ring を追加。fetch に res.ok+配列ガード |

### 補強テスト

- property-based シード探索（fast-check）でエンジン不変条件 + generate↔generateFrom 決定論を乱数シードで検証。
- ajv⇔zod 差分テスト（fast-check 500 ケース）で schema ドリフトを挙動一致で根絶。

検証: `npm run verify` 緑、293 tests pass、coverage 90.9/83.8/95.9/93.8（閾値 85/76/92/89）。

## 深堀調査 第2巡（113 アイデア / 8 領域並列 + 採点）→ 実装済み

100超のアイデアを学習科学/堅牢性/UX で深堀調査し、impact×fit/effort で採点した上位を実装した。

| ID | 内容 | 成果物 |
|---|---|---|
| D1 | weaknessScore の attempts 加点が逆転（練習量が多いほど弱点）→ 不確実(少試行)へ小ボーナスに是正 | `diagnosis.ts` |
| D2 | 正答率に Wilson 下側信頼区間（1/1 を満点扱いしない） | `diagnosis.ts` |
| SCHED-LEECH | lapses(永続化済・未使用)から leech 検出 | `leech.ts` |
| E4 | numeric answer を単位なし数値に強制（`Number(answer)` 採点の round-trip 保証） | `schema.ts`/`data-checks.ts` |
| quota-guard | `setItem` の QuotaExceededError ガード + ログ ring buffer（採点を黙って壊さない） | `store.ts` |
| DI-4 | 離散ドメイン（極数=偶数 / 周波数∈{50,60}） | `data-checks.ts` |
| DI-6 | past_exam の citation 書式（年度）検証 | `data-checks.ts` |
| DI-3 | 生成 params の realistic_range が宣言 paramSpecs と一致 | `template-invariants.test.ts` |
| SCHED-INTERLEAVE | 同 topic 連発を抑制し別論点を挟む（interleaving） | `select.ts` |
| PEDX-06 | 本日の復習キュー可視化（due/超過/ノルマ達成） | `queue.ts`/`app.ts` |
| D6 | 連続誤答の介入（解説強制/基礎回帰の促し） | `intervention.ts`/`app.ts` |
| E1 | distractor の reason を永続化し誤答時に「なぜ間違いか」を表示 | `schema.ts`/`generate.ts`/`app.ts` |
| CI-1 | 週次 cron 依存監査 | `audit-weekly.yml` |
| 統一 Scheduler | `LocalProgress` の scheduler 注入可能化（既定 SM-2） | `store.ts` |

### 深堀調査 追加実装（学習効果・a11y・データ保全・適応診断）

| ID | 内容 | 成果物 |
|---|---|---|
| SCHED-4LEVEL | 計測済 timeMs から4段階 Rating（速い=easy/遅い=hard）を導き record に配線 | `rating.ts` |
| A1 | 数式の読み上げ変換（√(²³×·Ωμ≈〔〕）+ statement/choices に aria-label | `math-speech.ts`/`app.ts` |
| export-import | 学習データの JSON 書き出し/取り込み/マージ（端末移行・消失保険） | `backup.ts`/`store.ts`/`app.ts` |
| persistent-storage | 意味あるデータ時のみ `navigator.storage.persist()` で eviction 防止 | `persist.ts`/`app.ts` |
| PEDX-01 | 誤答時の2回目挑戦（reveal 前 retry, record は最終1回） | `attempt.ts`/`app.ts` |
| D4 | mastery EWMA（直近重視の習熟度） | `mastery.ts` |
| D12 | topic 卒業/降格（mastery≥0.85 を弱点から除外・克服数表示） | `topic-state.ts`/`app.ts` |
| D3 | 前提科目順（理論優先で弱点を並べ替え） | `prereq.ts`/`app.ts` |
| SCHED-FSRS-WIRE | web 既定を FSRS へ切替（旧 SM-2 localStorage は `reviewStateToCard` で安全初期化。stability 無しは FSRS 未学習として復元し NaN を防止） | `store.ts`/`fsrs.ts` |
| SCHED-LAPSE-QUEUE | 問題単位の relearning キュー（誤答を再出題予定に積み、正解で卒業。`pickNext` が `dueLapses` を優先） | `lapse-queue.ts`/`store.ts`/`app.ts` |
| A3 | フィードバックを視覚（⭕❌絵文字）と読み上げ（mathToSpeech で記号読み下し・絵文字除去）に分離 | `grade.ts`/`app.ts` |
| DI-7 | ParamSpec の離散ドメインを一級宣言（`ParamDomain`）化。テンプレ宣言が単一情報源で、データゲート既定表との整合と generate() の不変条件を test が担保（DI-4 のハードコード規則を宣言データへ昇格） | `param-domain.ts`/`templates/types.ts`/`induction-motor-speed.ts`/`data-checks.ts` |
| SW-CACHE-VERSION | SW キャッシュ版数を配信アセットの内容ハッシュで build:web が自動更新（手動の版上げ忘れ→旧 app shell 固着の潜在バグを排除）。committed は `__BUILD_HASH__` プレースホルダ、ビルドで `denken-os-<hash>` に置換 | `build-web.ts`/`sw.js` |
| SCHED-FSRS-STATE | （自己レビューで検出）FSRS の `state` を reps から復元すると Learning/Relearning を Review に誤分類し間隔が約10倍に膨張（誤答カードが翌日でなく約10日後に再出題）。`state` を ReviewState/行ストアで永続化往復し是正。Card 直接続行と往復後で同一スケジュールになる回帰テスト追加 | `types.ts`/`fsrs.ts`/`supabase-store.ts`/`0004_*.sql` |
| E4-BLANK | （自己レビューで検出）`numericAnswerIssue` が空/空白 answer を見逃す（`Number("")===0` は有限）→ 採点が全回答を 0 と誤一致。trim 非空を必須化しデータゲートで弾く | `data-checks.ts` |

## ロードマップ（未実装。リスク/契約変更/外部依存のため次段の別 PR）

- **DI-13/DI-5** validated 昇格の provenance（validated_by/at/method）を gate で必須化（既存データ補完が要る契約変更）。
- **IndexedDB 移行 / schema-version**（localStorage→IndexedDB の容量・移行 infra。SW キャッシュ版数の自動化は SW-CACHE-VERSION で実装済）。
- **Supabase magic-link auth**（バックエンド依存・本 PR の offline-first 範囲外）。

### 保守メモ
- zod v4 へ移行済（`z.toJSONSchema` 利用可）。JSON Schema codegen を入れるなら superRefine 条件は overlay 合成 + 差分テストで担保（直列化不可のため）。
